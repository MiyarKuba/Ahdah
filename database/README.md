# Database workspace

The existing PostgreSQL database is not represented or modified during the foundation phase. The `schema` and `documentation` directories are reserved for reviewed, non-secret database artifacts in later tasks.

Database integration must follow [the database rules](../docs/DATABASE_RULES.md). Do not place credentials, ad hoc dumps, or unreviewed schema changes here.
